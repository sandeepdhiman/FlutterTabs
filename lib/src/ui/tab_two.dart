import 'package:flutter/cupertino.dart';

class TabTwo extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
          child: Text('It is camera tab.',
            style: TextStyle(fontSize: 20.0),
          )
      ),
    );
  }
}